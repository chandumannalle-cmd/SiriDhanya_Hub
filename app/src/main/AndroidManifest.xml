<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="SiriDhanyaHub"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity android:name=".HomeActivity"/>
        <activity android:name=".MandiWatchActivity"/>
        <activity android:name=".RecipeLabActivity"/>
        <activity android:name=".HealthBenefitsActivity"/>
        <activity android:name=".DirectBuyActivity"/>
        <activity android:name=".RecipeDetailActivity"/>

    </application>

</manifest>